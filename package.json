{
  "name": "side-a-record-catalog",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.mjs",
    "start": "npm run build && python3 -m http.server 8080 --directory dist"
  },
  "dependencies": {
    "@zxing/browser": "0.1.5"
  }
}
